# Browser uses the same-origin /api proxy by default.
# Do NOT put TWELVE_DATA_API_KEY in this file.
# Only set NEXT_PUBLIC_API_BASE_URL if you intentionally want direct browser -> FastAPI access.
# NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8765

# Next.js server-side proxy target
BACKEND_API_URL=http://127.0.0.1:8765
